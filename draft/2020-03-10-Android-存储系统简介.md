* 1. 特定于应用的文件
    * 1.1 访问特定于应用的内部存储
    * 1.2 访问特定于应用的外部存储
    * 1.3 查询可用空间
* 2. 媒体文件
* 3. 文档和其他类型文件

---

Android 的存储系统可存储的内容包括：

1. 文件(包括：特定于应用的、媒体文件、文档和其他类型文件)
2. 键值对
3. 数据库

对键值对的操作使用 Shared Preference 来实现，参考官方文档：https://developer.android.google.cn/guide/topics/ui/settings/use-saved-values

Android 中使用 sqlite 数据库，对数据库的操作使用官方的 Room 库，参考官方文档：

本文只讲解文件系统，包括：特定于应用的文件、媒体文件、文档和其他类型文件。

## 1. 特定于应用的文件

#### 1.1 访问特定于应用的内部存储


#### 1.2 访问特定于应用的外部存储

外部存储卷是可移除的，所以首先需要判断外部存储卷的状态，是否是挂载好了，是否是可读可写的；然后有些设备会把内部存储分一块作为外部存储，而且还支持插入 SD 存储卡，那么就可能有多个外部存储，这就要求我们要选择一个主要的、首选的外部存储卷。




#### 1.3 查询可用空间

## 2. 媒体文件

## 3. 文档和其他类型文件


